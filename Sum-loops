/* Sum of n numbers with loop */
#include<conio.h>
#include<stdio.h>
int main()
{
 int i,n,s=0;
 clrscr();
 printf("enter an integer:");
 scanf("%d",&n);
 for(i=1;i<=n;i++);
 {
  s=s+i;
 }
 printf("sum is %d",s);
 getch();
 return(0);
}
